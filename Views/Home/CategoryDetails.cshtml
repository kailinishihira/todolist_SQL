@{
  Layout = "_Layout";
}

<div class="jumbotron">
  <h1>@Model["category"].GetName() Details</h1>
</div>

<div class="container">
  <ul>
    @foreach (var task in Model["tasks"])
    {
      <li>@task.GetDescription()</li>
    }
  </ul>
</div>
